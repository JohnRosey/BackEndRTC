const con = require('./db.config');

// Vos fonctions pour créer les tables ici...

function createTables() {
  // Vos appels à con.query(...) pour créer les tables ici...
}

module.exports = createTables;
